import 'package:flutter/material.dart';

/// {@template action_view}
/// Action is the entrypoint for the primary actions of Focus app,
/// such as searching things, adding an event, playing/pausing, etc.
/// {@endtemplate}
class ActionView extends StatelessWidget {
  /// {@macro action_view}
  const ActionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
