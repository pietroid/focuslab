import 'package:flutter/material.dart';

/// {@template pending_view}
/// The [PendingView] is the place where pending events/actions are shown
/// {@endtemplate}
class PendingView extends StatelessWidget {
  /// {@macro pending_view}
  const PendingView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
