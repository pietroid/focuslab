import 'package:app_ui/app_ui.dart';
import 'package:flutter/widgets.dart';

class UIExperiencePage extends StatelessWidget {
  const UIExperiencePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const DefaultScaffold(
      title: 'UI Experience',
      body: Center(
        child: Text('UI Experience Page'),
      ),
    );
  }
}
